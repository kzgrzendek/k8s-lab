apiVersion: gateway.networking.k8s.io/v1alpha3
kind: TLSRoute
metadata:
  name: keycloak-tls-route
  namespace: keycloak
spec:
  parentRefs:
  - name: k8s-lab
    namespace: istio-gateway
    sectionName: keycloak-tls-passthrough
  
  hostnames:
  - "keycloak.k8s-lab.local"
  
  rules:
  - backendRefs:
    - name: keycloak-service
      port: 443