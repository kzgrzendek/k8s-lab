apiVersion: trust.cert-manager.io/v1alpha1
kind: Bundle
metadata:
  name: k8s-lab-ca-secret
  namespace: cert-manager
spec:
  sources:
  - useDefaultCAs: true
  - secret:
      name: "k8s-lab-cacert"
      key: "tls.crt"
  target:
    secret:
      key: "cert.pem"
    namespaceSelector:
      matchLabels:
        trust-manager/inject-lab-ca-secret: "enabled"