apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: gateway-lab-tls
  namespace: envoy-gateway-system
spec:
  secretName: gateway-lab-tls
  issuerRef:
    name: k8s-lab-issuer
    kind: ClusterIssuer
  commonName: "*.lab.k8s.local"
  dnsNames:
  - "*.lab.k8s.local"